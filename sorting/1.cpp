#include <bits/stdc++.h>
#define vi vector<int>
#define out(x) cout << x << endl
#define civ(x, n) vi x(n);for (int i = 0; i < n; i++) cin >> x[i]
using namespace std;
using ll = long long;
#define int long long
#define si set<int>
#define pb push_back
#define ump unordered_map<int, int>
#define pii pair<int, int>
#define def(x, n) int n;cin>>n;vi x(n);for (int i = 0; i < n; i++) cin >> x[i]
 
const int MOD = 998244353;
 
 
void iota_pi(){
  def(a,n);
  si ch;
  for(int i:a){
    ch.insert(i);
  }out(ch.size());
 
 
 
}signed main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int t=1;//cin >> t;
  while (t--) iota_pi();
